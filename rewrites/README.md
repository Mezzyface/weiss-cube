# Rewrite Files

Each .json file here is produced by a haiku agent.
Format: {"profile_id": "rewritten_effect_text", ...}

Review with: python apply_rewrites.py
Auto-apply:  python apply_rewrites.py --auto

Files are moved to applied/ after processing.
